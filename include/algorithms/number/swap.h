#pragma once

#include "algorithms/error_code.h"

/**
 * Time complexity -- all cases: O(1)
 * Space complexity -- all cases: O(1)
 */
error_code number_swap(int * number1, int * number2);

error_code number_swap_using_xor(int * number1, int * number2);
